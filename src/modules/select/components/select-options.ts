import { Component, Input } from "@angular/core";

@Component({
    selector: "sui-select-options",
    template: ``,
    styles: [`
:host {
    display: none;
}
`]
})
export class SuiSelectOptions<T> {}
