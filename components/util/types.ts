export type SuiColor = "" | "red" | "orange" | "yellow" | "olive" | "green" | "teal" | "blue" | "violet" | "purple" | "pink" | "brown" | "black";

export const SuiColor = {
    Default: "" as SuiColor,
    Red: "red" as SuiColor,
    Orange: "orange" as SuiColor,
    Yellow: "yellow" as SuiColor,
    Olive: "olive" as SuiColor,
    Green: "green" as SuiColor,
    Teal: "teal" as SuiColor,
    Blue: "blue" as SuiColor,
    Violet: "violet" as SuiColor,
    Purple: "purple" as SuiColor,
    Pink: "pink" as SuiColor,
    Brown: "brown" as SuiColor,
    Black: "black" as SuiColor
}

export type SuiSize = "mini" | "tiny" | "small" | "" | "large" | "big" | "huge" | "massive";

export const SuiSize = {
    Mini: "mini" as SuiSize,
    Tiny: "tiny" as SuiSize,
    Small: "small" as SuiSize,
    Default: "" as SuiSize,
    Large: "large" as SuiSize,
    Big: "big" as SuiSize,
    Huge: "huge" as SuiSize,
    Massive: "massive" as SuiSize
}